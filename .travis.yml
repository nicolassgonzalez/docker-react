language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t nicolassgonzalez/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nicolassgonzalez/docker-react npm run test -- --coverage
